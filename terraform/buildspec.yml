version: 0.2
phases:
  build:
    commands:
      - echo "Syncing website files to S3 bucket..."
      - aws s3 sync --delete ./website/ s3://${DOMAIN_NAME}/
      - echo "Invalidating CloudFront cache..."
      - aws cloudfront create-invalidation --distribution-id ${CLOUDFRONT_ID} --paths "/*"